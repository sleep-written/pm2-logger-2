/**
 * Public surface of the `environment` feature: the {@link Environment} class
 * for typed `.env`-based configuration, plus its supporting interfaces.
 *
 * @module
 */
export * from './interfaces/index.js';
export { Environment } from './environment.js';